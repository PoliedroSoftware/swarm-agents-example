# Frontend-specific. Angular reads these via environment.ts files at build time
# (we'll wire a small env-to-environment.ts generator in Phase 6).

API_BASE_URL=http://localhost:5000
NODE_ENV=development
